# build output and installed dependencies
node_modules
dist
storybook-static
playwright-report
test-results
e2e/.auth
e2e/.output

# lockfile: bun owns the formatting, a rewrite fights `bun install`
bun.lock

# generated by `bun run gen:rbac` from the control plane's rbac matrix;
# the generator owns its formatting, not the formatter
src/lib/rbac-capabilities.json
